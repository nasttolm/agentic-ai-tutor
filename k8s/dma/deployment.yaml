apiVersion: apps/v1
kind: Deployment
metadata:
  name: dma-service
  namespace: ai-tutor
  labels:
    app: ai-tutor
    subject: dma
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-tutor
      subject: dma
  template:
    metadata:
      labels:
        app: ai-tutor
        subject: dma
    spec:
      containers:
        - name: dma-service
          image: ghcr.io/nasttolm/ai-tutor-backend:latest
          ports:
            - containerPort: 8000
          env:
            - name: SUBJECT
              value: "dma"
            - name: CHROMA_HOST
              value: "dma-chroma"
            - name: CHROMA_PORT
              value: "8000"
          resources:
            requests:
              memory: "4Gi"
              cpu: "1"
              nvidia.com/gpu: "1"
            limits:
              memory: "12Gi"
              cpu: "4"
              nvidia.com/gpu: "1"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 120
            periodSeconds: 30
      nodeSelector:
        nvidia.com/gpu: "true"
      tolerations:
        - key: "nvidia.com/gpu"
          operator: "Exists"
          effect: "NoSchedule"
