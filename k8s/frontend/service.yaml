apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ai-tutor
  labels:
    app: ai-tutor
    component: frontend
spec:
  selector:
    app: ai-tutor
    component: frontend
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
