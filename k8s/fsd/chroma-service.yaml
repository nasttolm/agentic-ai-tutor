apiVersion: v1
kind: Service
metadata:
  name: fsd-chroma
  namespace: ai-tutor
  labels:
    app: fsd-chroma
    subject: fsd
spec:
  selector:
    app: fsd-chroma
  ports:
    - port: 8000
      targetPort: 8000
